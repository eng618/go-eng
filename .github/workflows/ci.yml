name: build-test-lint

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    tags: [v*]
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test_matrix:
    name: Go ${{ matrix.go }} – test/build
    runs-on: ubuntu-latest
    permissions:
      contents: read
    # each matrix job must finish quickly; prevents hung builds from
    # consuming runners indefinitely
    timeout-minutes: 30
    strategy:
      matrix:
        go: ["1.24", "1.25", "1.26"]
      max-parallel: 4 # run all versions at once up to this limit
    env:
      # older releases don't understand a go.work file; disable it so the
      # checkout behaves like a single-module project.  we validate the
      # repository against multiple Go versions so we know when API/encoding
      # changes break backwards compatibility.
      GOWORK: off
      # web-service-gin is built with go 1.26 in its own module; tests/ build
      # should be skipped on older Go to avoid "requires go >= 1.26" errors.
      SKIP_WEB_SERVICE: ${{ matrix.go != '1.26' }}
    steps:
      - uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ matrix.go }}

      - name: Install Task
        uses: arduino/setup-task@v2
        with:
          version: 3.x
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Test
        run: task test

      - name: Build
        run: task build

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: ./coverage.out

  lint:
    name: Lint (latest Go)
    runs-on: ubuntu-latest
    needs: test_matrix
    permissions:
      contents: read
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: 1.26

      - name: Install Task
        uses: arduino/setup-task@v2
        with:
          version: 3.x
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          # keeping "latest" here ensures the action itself keeps up with
          # newer lint releases; it's run on the newest Go so compatibility
          # problems from the matrix are already covered by the previous job.
          version: latest

      - name: Lint
        run: task lint
